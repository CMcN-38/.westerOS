#!/usr/bin/env bash

# Launch the TUI command passed in as an argument in the default terminal with an org.omarchy.COMMAND app id for styling.

exec setsid uwsm-app -- kitty --app-id=org.westerOS.$(basename $1) -e "$1" "${@:2}"
