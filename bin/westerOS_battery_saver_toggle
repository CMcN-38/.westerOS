#!/usr/bin/env bash

# Battery Saver Mode - Toggle Script
# Toggles battery saver mode on/off.

STATE_FLAG="/run/user/$UID/westerOS_battery_saver"

if [ -f "$STATE_FLAG" ]; then
    westerOS_battery_saver_disable
else
    westerOS_battery_saver_enable
fi
